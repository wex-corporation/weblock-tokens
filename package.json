{
  "name": "weblock-contracts",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf artifacts cache typechain",
    "build": "npm run clean && hardhat compile",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "typechain": "hardhat compile && typechain",
    "lint:sol": "solhint 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.9.1",
    "@types/node": "^25.0.3",
    "hardhat": "^2.22.0",
    "rimraf": "^6.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
